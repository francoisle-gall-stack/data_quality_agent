schema_version: 2
scenario_id: SC037
difficulty_level: 5
level: expert
name: Intermittent incremental MERGE failure caused by overlapping SCD2 customer versions
description: fct_orders fails on recent orders because overlapping active dim_customers versions multiply order_id during incremental MERGE; full-refresh remains valid.
patch_files:
  - dbt/models/2_marts/dim_customers.sql
  - dbt/models/2_marts/fct_orders.sql
