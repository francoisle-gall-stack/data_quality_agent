schema_version: 2
scenario_id: SC018
difficulty_level: 3
level: macro
name: Missing revenue macro
description: int_order_revenue calls a project macro that is not present, causing dbt compilation to fail
patch_files:
  - dbt/models/1_intermediate/int_order_revenue.sql
